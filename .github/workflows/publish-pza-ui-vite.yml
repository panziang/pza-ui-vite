name: Publish pza-ui-vite To Npm

on:
  push:
    branches: [publish-pza-ui-vite]

jobs:
  publish:
    runs-on: ubuntu-latest

    name: "publish npm"

    environment: npm

    steps:
      - uses: actions/checkout@master
      - uses: pnpm/action-setup@v2.1.0
        with:
          version: 6.31.0
      - name: Install modules
        run: pnpm install
      - name: Build
        run:  npm run build
      - name: "Publish to the npm registry"
        uses: primer/publish@3.0.0
        env:
          GITHUB_TOKEN: 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
          NPM_AUTH_TOKEN: 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 # 跟前面步骤中的 NPM_AUTH_TOKEN 保持一致
        with:
          default_branch: "publish"
          